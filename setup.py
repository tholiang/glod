from setuptools import setup, find_packages
from setuptools import setup, find_packages

setup(
    name='glod',
    version='0.1.0',
    packages=find_packages(where='src'),
    package_dir={'': 'src'},
)

setup(
    name='glod',
    version='0.1.0',
)